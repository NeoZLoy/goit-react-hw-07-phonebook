import { styled } from "styled-components";

const StyledFilter = styled.div`
display: flex;
flex-direction: column;
gap: 8px
`

const StyledTitle = styled.h3`
padding: 0;
margin: 0;
`



export {StyledFilter, StyledTitle}

